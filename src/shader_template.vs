#version version_number

in type  in_varaiable_name
in type  in_varaiable_name

out type out_variable_name

uniform type uniform_name;

void main{
    // process inputs and  so some weried graphics stuff
    ....
    // output processed stuff to ouput variable
    out_variable_name= weried_stuff_we_processed 
}